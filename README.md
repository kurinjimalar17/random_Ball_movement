This project is a simple **Random Ball Movement** program using HTML, CSS, and JavaScript.
It creates three colored balls that move inside a fixed area.
Each ball moves at a different speed and changes its direction when it reaches the boundary.
